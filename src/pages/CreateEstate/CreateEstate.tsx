import { Box } from "@mui/material";
import { CreateEstateModule } from "../../modules/CreateEstateModule";

export const CreateEstate = () => {
  return (
    <Box className="section">
      <CreateEstateModule />
    </Box>
  );
};
